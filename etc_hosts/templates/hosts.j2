# Ansible provided.

127.0.0.1 localhost
::1 localhost

{% for host in groups['all']  %}
{% if hostvars[host].ansible_default_ipv4.address is defined %}
{{ hostvars[host].ansible_default_ipv4.address }} {{ hostvars[host].ansible_ssh_host }} {{ hostvars[host].ansible_hostname }}
{% endif %}
{% endfor %}
